import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class BattleService {
  // Methods for calculating battle readiness and predicting winners will be added here.
}
